{
  "name": "library-management-system",
  "version": "1.0.0",
  "description": "A REST API for a library management system",
  "main": "app.js",
  "scripts": {
    "start": "export NODE_ENV=production && sequelize-cli db:migrate && sequelize-cli db:seed:all && node app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shubhankar Mane",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "helmet": "^4.5.0",
    "jsonwebtoken": "^8.5.1",
    "mysql2": "^2.2.5",
    "nodemon": "^2.0.7",
    "sequelize": "^6.6.2",
    "sequelize-cli": "^6.2.0"
  }
}
